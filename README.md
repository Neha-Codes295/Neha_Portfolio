# Portfolio (Next.js)

Deploy on Vercel from this repository root (no subfolder “Root Directory” needed).

Set `NEXT_PUBLIC_SITE_URL` to your live URL (no trailing slash). Optional: `NEXT_PUBLIC_RESUME_URL`, `NEXT_PUBLIC_GITHUB_URL`.

```bash
npm install
npm run dev
npm run build
```
